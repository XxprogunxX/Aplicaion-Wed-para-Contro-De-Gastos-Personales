# Desarrollo (docker-compose con volúmenes): incluye devDependencies (nodemon, etc.)
FROM node:20-bookworm-slim

WORKDIR /app

COPY package*.json ./
RUN npm ci && npm cache clean --force

COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
